<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :details %>
  <%= f.text_field :details %>

  <% unless @task.new_record? %>
    <%= f.label :completed %>
    <%= f.text_field :completed %>
  <% end %>

  <%= f.submit %>
<% end %>
